{
    "name": "mon-projet-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && mkdir -p public && cp -r frontend/dist/* public/ && cd backend && npm install"
    },
    "workspaces": [
        "frontend",
        "backend"
    ],
    "dependencies": {
        "@supabase/supabase-js": "^2.87.1",
        "cors": "^2.8.5",
        "dotenv": "^16.6.1",
        "express": "^4.22.1"
    }
}